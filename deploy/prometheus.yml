global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: malaysia-data-mcp
    metrics_path: /metrics
    static_configs:
      - targets:
          - server:8000
        labels:
          service: malaysia-data-mcp
          env: local
